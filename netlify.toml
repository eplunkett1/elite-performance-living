[build]
  command = "npm run build"
  publish = "dist"

# API must be matched before the SPA fallback
[[redirects]]
  from = "/api/anthropic"
  to = "/.netlify/functions/anthropic-proxy"
  status = 200
  force = true

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
